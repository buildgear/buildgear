Requirements:

   g++
   libcurl4-gnutls-dev
   termcap

   Note: termcap is usually bundled with ncurses/curses.

Installation:

   ./configure
   make
   make install

   See ./configure --help for more options.
